# API base URL. Leave empty when frontend is served by the same origin as the backend.
# Set to the backend URL (e.g. https://office-shift-manager.onrender.com) when the
# frontend is deployed separately.
VITE_API_URL=
